# ======================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ======================================
# Copy this file to .env.prod and fill in actual values
# DO NOT commit .env.prod to git!

# ======================================
# SECURITY (CHANGE THESE!)
# ======================================
DEBUG=False
SECRET_KEY=your-random-50-character-secret-key-here-CHANGE-THIS

# If you have a domain:
# ALLOWED_HOSTS=your-domain.com,www.your-domain.com
# If NO domain (use VPS IP):
ALLOWED_HOSTS=123.456.789.10,localhost,127.0.0.1

# ======================================
# DATABASE (MySQL in Docker)
# ======================================
DB_ENGINE=django.db.backends.mysql
DB_NAME=e_commerce
DB_USER=admin
DB_PASSWORD=YOUR-STRONG-DB-PASSWORD-HERE
DB_HOST=db
DB_PORT=3306

MYSQL_ROOT_PASSWORD=YOUR-STRONG-ROOT-PASSWORD-HERE
MYSQL_DATABASE=e_commerce
MYSQL_USER=admin
MYSQL_PASSWORD=YOUR-STRONG-DB-PASSWORD-HERE

# ======================================
# REDIS CACHE (Redis in Docker)
# ======================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# ======================================
# BACKEND
# ======================================
DJANGO_PORT=8000

# ======================================
# FRONTEND (Public URLs)
# ======================================
# If you have a domain:
# NEXT_PUBLIC_API_URL=https://your-domain.com/api
# NEXT_PUBLIC_WS_HOST=your-domain.com
# FRONTEND_URL=https://your-domain.com

# If NO domain (use VPS IP):
NEXT_PUBLIC_API_URL=http://123.456.789.10/api
NEXT_PUBLIC_WS_HOST=123.456.789.10
FRONTEND_URL=http://123.456.789.10

# ======================================
# STRIPE PAYMENT (Live Keys!)
# ======================================
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_YOUR_STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_STRIPE_PUBLISHABLE_KEY
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_STRIPE_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET

# ======================================
# NOTES
# ======================================
# 1. Generate SECRET_KEY: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# 2. Use STRONG passwords (min 16 chars, mix of letters/numbers/symbols)
# 3. Replace '123.456.789.10' with your actual VPS IP address
# 4. If you have a domain, use https:// URLs; if NO domain, use http:// with IP
# 5. Use LIVE Stripe keys (sk_live_..., pk_live_...) for production
# 6. Never commit .env.prod to git - add to .gitignore
# 7. To get SSL with IP (optional): Use self-signed cert or Cloudflare
